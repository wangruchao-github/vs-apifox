1. @apiFolder 注解没有正确识别

2. 接口名称识别异常

```java
// ==================== @PathVariable 测试 ====================

    /**
     * 根据ID获取用户 - 单个路径参数
     */
    @GetMapping("/{id}")
    public ApiResponse<User> getUserById(@PathVariable Long id) {
        User user = new User(id, "user" + id, "user" + id + "@example.com", 25, "13800138000");
        return ApiResponse.success(user);
    }
```

现在识别到的是 ==================== @PathVariable 测试 ====================
期望是 根据ID获取用户 - 单个路径参数

3. 没有识别到 “@RequestBody List<Long> ids” 

```java
/**
 * 批量删除用户
 */
@DeleteMapping("/batch")
public ApiResponse<Void> batchDeleteUsers(@RequestBody List<Long> ids) {
    return ApiResponse.success("批量删除成功，共删除 " + ids.size() + " 条记录", null);
}
```

4. 响应数据结构没有识别到

5. 响应数据识别到的data现在都是object类型，没有识别到泛型类的结构

```java
@GetMapping("/{id}")
public ApiResponse<User> getUserById(@PathVariable Long id) {
    User user = new User(id, "user" + id, "user" + id + "@example.com", 25, "13800138000");
    return ApiResponse.success(user);
}
```